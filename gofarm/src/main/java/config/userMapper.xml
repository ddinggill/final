<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper    
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"    
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
 	<insert id="signup" parameterType="com.mycompany.gofarm.user.dto.UserDTO">
 		insert into users 
 		values(users_seq.nextval, #{userid}, #{userpw}, #{name}, 
 			#{birth}, #{phone}, #{email}, #{nickname}, 0)
 	</insert>
 	
 	<insert id="signupdetail" parameterType="com.mycompany.gofarm.user.dto.UserDTO">
 		insert into user_detail
 		values(users_seq.currval , #{bank}, #{account}, 0, #{user_home})
 	</insert>

	<!-- <insert id="signup" parameterType="com.mycompany.gofarm.user.dto.UserDTO">
		insert into users_test
		values(users2_seq.nextval)
	</insert>

	<insert id="signupdetail" parameterType="com.mycompany.gofarm.user.dto.UserDTO">
		insert into usersdetail_test
		values(users2_seq.currval)
	</insert> -->
</mapper>
